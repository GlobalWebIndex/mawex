sudo: required
os:
  - linux
services:
  - docker
language: scala
scala:
  - 2.12.3
jdk:
  - oraclejdk8
cache:
  directories:
    - $HOME/.m2/repository
    - $HOME/.sbt
    - $HOME/.ivy2
branches:
  only:
    - master
notifications:
  email:
    recipients:
      - liska.jakub@gmail.com
before_install:
    - docker pull dwmkerr/dynamodb
    - docker pull garland/aws-cli-docker
before_script:
  - rm -rf ${HOME}/.sbt/0.13/staging
script:
  - sbt ++$TRAVIS_SCALA_VERSION clean mawex-example-worker/test