sudo: required
os:
  - linux
services:
  - docker
language: scala
scala:
  - 2.12.4
jdk:
  - oraclejdk9
cache:
  directories:
    - $HOME/.m2/repository
    - $HOME/.sbt
    - $HOME/.ivy2
branches:
  only:
    - master
notifications:
  email:
    recipients:
      - liska.jakub@gmail.com
before_script:
  - rm -rf ${HOME}/.sbt/1.0/staging
script:
  - sbt ++$TRAVIS_SCALA_VERSION clean Mawex-core/test